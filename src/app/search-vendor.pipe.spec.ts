import { SearchVendorPipe } from './search-vendor.pipe';

describe('SearchVendorPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchVendorPipe();
    expect(pipe).toBeTruthy();
  });
});
